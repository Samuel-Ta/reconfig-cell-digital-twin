# colcon build products (regenerated by `colcon build`)
/build/
/install/
/log/

# compiled fastcdr shim — rebuildable from fastcdr_compat.c
# (gcc -shared -fPIC -o fastcdr_compat.so fastcdr_compat.c -ldl)
*.so

# python
__pycache__/
*.py[cod]

# editor / os cruft
*.swp
*~
.vscode/
.DS_Store
